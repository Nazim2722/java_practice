count = 0;  
   
#Opens a file in read mode  
file = open("que1.py", "r")  
      
#Gets each line till end of file is reached  
for line in file:  
    #Splits each line into words  
    words = line.split(" ");  
    #Counts each word  
    count = count + len(words);  
    count = count + len(total number of line)
    print(count)
print("“this is what the file would look like as the file is what it look and what it would do for you to enable reading the file this is what you do .” + str(count));  
     file.close();  